# `babel-udp`
Run the babel routing protocol from `babel-proto` over UDP.

This crate is provided for user convenience if you want to run babel in a std environment over UDP. 

It is also used to validate the protocol against implementations in other languages, such as:
- babeld (C)
- pybabel (Python)
- go-babel (Golang)
